import Header from "@/components/header/header";

const Home = () => {
  return (
    <section className="h-screen w-full bg-black xl:container">
      <Header />
      <main>main page</main>
    </section>
  );
};

export default Home;
